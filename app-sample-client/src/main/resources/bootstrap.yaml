dubbo:
  cloud:
    subscribed-services: app-sample-server
  protocols:
    dubbo:
      port: -1

spring:
  application:
    name: app-sample-client
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        username: nacos
        password: nacos
        server-addr: app-nacos:8848
        namespace: app-cloud-dubbo

server:
  port: 8001