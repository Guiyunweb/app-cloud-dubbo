server:
  port: 8000

spring:
  application:
    name: app-gateway
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      server-addr: app-nacos:8848
      discovery:
        namespace: app-cloud-dubbo