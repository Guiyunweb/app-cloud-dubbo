dubbo:
  cloud:
    subscribed-services: app-server
  protocols:
    dubbo:
      port: -1

spring:
  application:
    name: app-client
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        username: nacos
        password: nacos
        server-addr: 127.0.0.1:8848
        namespace: public

server:
  port: 8001